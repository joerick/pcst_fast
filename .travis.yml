matrix:
  include:
    - sudo: required
      services:
        - docker
    - os: osx

script:
  - pip install git+https://github.com/joerick/cibuildwheel@3f6a943b6d6a1e47a3c4f7e6930dadd171374a22
  - cibuildwheel --output-dir wheelhouse

env:
  global:
    - CIBW_BEFORE_BUILD="{pip} install pybind11"
    - CIBW_SKIP=cp26-*

# language: cpp
# compiler:
#   - gcc
#   - clang
# os:
#   - linux
#   - osx
# dist: trusty

# script: "make pcst_fast_test && ./pcst_fast_test"

# notifications:
#   recipients:
#     - ludwigschmidt2@gmail.com
#   email:
#     on_success: never
#     on_failure: change
